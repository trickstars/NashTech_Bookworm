// src/types/author.ts (File mới hoặc thêm vào file types chung)
export interface Author {
    id: number | string; // ID để dùng làm value khi filter
    authorName: string;        // Tên hiển thị
  }